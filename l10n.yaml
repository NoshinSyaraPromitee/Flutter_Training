# Optional: only needed if you later switch from the hand-written
# lib/l10n/app_localizations*.dart files to Flutter's official codegen
# (flutter gen-l10n / `generate: true` in pubspec.yaml).
arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
synthetic-package: false
